# 숙소·객실 마스터 콘솔 — 구현

Claude Design 핸드오프(`project/숙소객실 마스터.dc.html`)를 React + TypeScript + Vite로 구현한 결과물입니다.
디자인 원본과 대화 기록은 `README.md` · `chats/` · `project/`에 그대로 두었습니다.

```bash
npm install
npm run dev        # http://localhost:5173
npm run build      # 타입체크 + 프로덕션 빌드
npm test           # 전체 108건 (도메인 · 렌더 · 시뮬레이션)
npm run simulate   # 직원 시나리오 시뮬레이션 리포트 출력
npm run bundle     # 임베드용 단일 HTML 파일 (dist/single-file.html)
```

데이터는 인메모리 목데이터입니다. **서로 모양이 다른 숙소 3곳**이 `src/domain/seed.ts`에 들어 있고, 새로고침하면 초기 상태로 돌아갑니다.

| 숙소 | 객실 | 이 숙소가 쓰는 값 | 없는 것 |
|---|---|---|---|
| 속초 더샵 스파 펜션 (2656) | 28 | 기준/최대 인원 · 추가인원 요금 · 바베큐 · 스파 · 전망 · 객실 비품 | 캠핑 · 개별풀 · 애견 |
| 가평 리버뷰 풀빌라 (1042) | 12 | 기준/최대 인원 · 추가인원 요금 · 개별 수영장 · 반려동물 · 전망 | 바베큐 · 스파 |
| 홍천 카라반파크 (3471) | 24 | 기준/최대 인원 · 추가인원 요금 · 캠핑 자리 · 바베큐 · 전망 | 스파 · 수영장 |

세 곳은 열 구성도, 요금표도, 판매 사이트로 나가는 말도 서로 다릅니다. **그런데 화면 코드는 한 벌뿐입니다** —
열·요금표·연쇄 갱신·검사·채널 변환이 전부 숙소가 선언한 속성에서 만들어지기 때문입니다.

## 구조

```
src/
  domain/        React 비의존 순수 로직 — 여기가 이 화면의 핵심입니다
    types.ts       도메인 타입 (AttrDef · Property · Room · Block · Rule · Cascade …)
    attrs.ts       전사 속성 사전 — 숙소가 가질 수 있는 값의 정의. 여기가 확장 지점입니다
    catalog.ts     전사 카탈로그: 필드 형식 규칙, 안내 문구 패턴, 채널 키
    seed.ts        서로 모양이 다른 숙소 3곳
    fieldTypes.ts  값 형식 판별(typeOf) + 문자열↔구조값 변환(parseVal/composeVal)
    derive.ts      객실에서 자동 계산되는 시설 필드 · 연쇄 diff · 채널 행 계산
    cascade.ts     연쇄 갱신 미리보기 생성 + 적용(commit)
    faq.ts         시설 값을 인용하는 FAQ 답변 생성
    audit.ts       일관성 검사 — 숙소마다 매 작업 후 지켜져야 하는 규칙 14가지
  state/store.tsx  reducer + Context. 모든 쓰기는 미리보기 → 적용 → 되돌리기를 지납니다
  components/      화면. tabs/ 6개 · modals/ 4개 · 좌측 숙소 목록 · 우측 전체값 패널
  styles/          ds-industry.css(디자인 시스템 원본 그대로) + global.css
```

원본 프로토타입은 `sc-for` / `sc-if` / `{{ }}` 템플릿과 `DCLogic` 클래스로 되어 있었습니다. 화면은 픽셀 단위로 옮기되
(인라인 스타일·그리드 컬럼·폰트 크기 그대로), 로직은 순수 함수(`domain/`)와 reducer로 재구성했습니다. 하드코딩된 hex는
0개이며 색·간격은 전부 `ds-industry.css` 토큰에서 옵니다. 프로토타입의 `style-hover` 속성은 `global.css`의 hover 클래스
4개로 대체했습니다.

## 설계 원칙 — 어긋날 수 있는 입력 자체를 없애기

- **객실이 무엇을 가지는지는 타입이 아니라 데이터가 정합니다.** `Room`에 `bbq`·`spa` 같은 열이 박혀 있으면
  캠핑장만 있는 숙소는 담기지 않습니다. 전사 속성 사전(`domain/attrs.ts`)이 값을 정의하고, 숙소는 `attrs`로
  자기가 쓰는 것만 고르며, 객실은 `values`에 **기본값과 다른 것만** 적습니다.
  새 시설이 생기면 사전에 한 줄 넣으면 되고 — 열·편집기·연쇄 갱신·검사·판매 사이트 변환이 모두 따라옵니다.
- **끝이 없는 값은 목록으로 만들지 않습니다.** 인원·요금은 숫자 입력입니다. 목록으로 만드는 순간
  "그 밖의 값"을 넣을 방법이 사라지고, 1000개 중 몇 곳은 반드시 그 밖에 있습니다.
  대신 숫자에서만 생기는 실수(기준 인원 > 최대 인원, 음수 요금, 범위 밖)는 검사기가 잡습니다.
- **숙소값 = 기본값, 객실 = 오버라이드** (단방향). 오버라이드는 액센트 마커, 상속은 점선 밑줄.
- **자동 산출 필드**는 저장된 문장이 아니라 규칙입니다. 블록의 `이용 객실`·`이용 요금`·`바베큐 형태`, 스파 `수용인원`은
  객실 테이블에서 매번 계산되고, 수정 버튼이 없으며, 미리보기에서도 체크 해제가 불가능한 잠금 행으로 나옵니다.
- **요금은 객실이 아니라 옵션에 붙습니다.** 옵션 요금 한 번 수정 → 그 옵션을 쓰는 모든 객실·블록 문구·채널 요금이 함께 갱신.
- **FAQ 답변 7건**은 시설 값 템플릿에서 생성됩니다(`{shared_bbq.이용 시간}에 이용 가능합니다.`). 따로 입력할 곳이 없으므로
  시설과 어긋날 수 없고, 시설이 미사용이 되면 자동 비활성됩니다.
- **모든 값은 형식/옵션 입력**입니다: 시간 · 시간대(시작 기준 "입실" 포함) · 날짜 · 금액 · 금액 구간(과금 기준/결제 방식) ·
  정수(비교 조건) · 옵션. 자유 텍스트는 주의사항·안내처럼 이름이 문장인 필드만 도달합니다.
- **안내 규칙**은 전사 카탈로그에서 고르고 조각(시각·금액·수치·옵션·고유명사)만 채웁니다 — 문장은 생성됩니다.
  주의사항·특이사항·교통·시설 안내에 남아 있던 통문장이 전부 이 구조로 넘어갔습니다.

## 프로토타입 대비 달라진 점

구현하면서 확인된 원본의 불일치와, 사용자가 요청한 추가 사항입니다.

1. **공용BBQ 객실 수 24 → 22.** 원본 객실표는 7층 4실 + 3층 6실 + 4~6층 각 6실 = 28실이므로 공용BBQ(4~7층)는 22실입니다.
   프로토타입에 적혀 있던 "24객실"은 객실표와 맞지 않는 손으로 쓴 숫자였습니다. 시드 값과 채널 매핑 문구를 22로 맞췄고,
   화면에 보이는 값은 항상 객실 테이블에서 계산됩니다.
2. **금액 파싱 버그 수정.** `1세트 20,000원`에서 숫자만 추출하면 "1세트"의 1이 붙어 120,000원이 되던 문제 — 옵션 요금
   편집창을 열 때마다 금액이 부풀려졌습니다. 이제 `원` 앞의 금액을 기준으로 읽습니다.
3. **미리보기와 저장이 같은 요금을 씁니다.** 미리보기의 시뮬레이션이 요금을 하드코딩하고 있어서, 저장 시 실제로 기록되는
   옵션 요금과 다른 숫자를 보여줄 수 있었습니다. 이제 양쪽 다 옵션 표에서 읽습니다.
4. **안내 규칙 UI 신규 구현.** 원본에는 로직(`RULECAT`·추가/삭제/조각 편집)만 있고 화면이 없었습니다. 사용중 시설 카드에
   규칙 목록 · 조각 편집 · 카탈로그에서 규칙 추가를 붙였습니다.
5. **표시 설정 노출.** 프로토타입 Tweaks 3개(상속 표현 marker/ghost · 연쇄 갱신 preview/instant · 레이아웃 3단/2단)를
   헤더의 "표시 설정" 메뉴로 꺼냈습니다. `즉시 적용`은 원본에서 버튼 라벨만 바뀌었지만, 설정으로 노출한 이상 실제로
   미리보기를 건너뛰고 바로 적용합니다(되돌리기는 그대로 유지).
6. **블록 카드가 항상 파생값을 표시합니다.** 원본은 저장된 문자열을 그대로 보여주다가 변경이 일어나야 재계산했습니다.
7. **신규 객실 코드**는 난수 대신 기존 최대 코드 + 1을 씁니다(중복 방지).
8. **인스펙터 "영향 범위" 수치**를 고정값이 아니라 현재 객실 데이터에서 계산합니다.
9. **모달은 네이티브 `<dialog>` + `showModal()`** 입니다. `position: fixed` 오버레이는 조상에 `transform`·`filter`·
   `contain`·`will-change` 중 하나만 있어도 그 조상 기준으로 바뀌어 본문 흐름에 섞입니다(페이지를 감싸는 임베드
   호스트가 흔히 그렇습니다). top layer에 그리면 어디에 얹히든 화면 중앙에 뜨고, Esc 닫기·포커스 트랩이 따라옵니다.
   되돌리기 토스트도 같은 이유로 `<body>`에 포털합니다.

## 조각화 — 통문장을 없앤 결과

원본에서 `note`(※ 주의사항 문단)와 스냅샷의 `특이사항`·`교통`·`숙소 시설` 통문장은 숫자·시각·요금이 문장 안에 박혀 있어
시설 값과 어긋나도 아무도 모르는 자리였습니다. 이제 전부 **규칙 조각**입니다.

- `RULECAT` 45개 패턴 · 7개 그룹(이용 조건 · 안전 · 요금 · 예약·입실 · 객실 이용 · 시설 안내 · 주변·교통)
- 조각 형식: `시각` · `금액` · `정수+단위` · `시간대` · `옵션` · `text`(상호명·지명 같은 고유명사 한 낱말)
- 블록별로 쓸 수 있는 규칙만 목록에 뜨고, `여러 건` 표시가 붙은 규칙(주변 여행지·미비치 품목 등)만 중복 추가됩니다
- 새 블록 3개가 조각으로 들어왔습니다: **특이사항 · 이용 정책**(12규칙) · **숙소 시설 안내**(9규칙) · **교통 · 주변**(7규칙)
- `Block.note`(자유 문단) 필드는 삭제했습니다. 화면에도 코드에도 통문장을 담을 자리가 없습니다

예: `※ 22시 이후 고성방가 자제` → 규칙 `{time} 이후 고성방가 자제` + 시각 조각 `22:00`.
`※ 퇴실 지연 시 1시간 초과 5만 원` → `퇴실 지연 시 사전 요청 · {n} 초과 시 {amt} 추가` + `1시간` + `50,000원`.
`- 등대해수욕장(차량 약 9분)` → `{name} 차량 약 {n}` + `등대해수욕장` + `9분`.

자유 텍스트로 남은 것은 **주소 2건**(지번·도로명)뿐입니다. 주소는 문장 안에 요금·시각이 섞이지 않아 어긋날 여지가 없습니다.

## 시뮬레이션 — 직원이 만지면 어떻게 되는가

`npm run simulate`는 실제 화면이 보내는 액션 순서 그대로 상태를 굴리고, **매 단계 끝에서 일관성 검사**(`domain/audit.ts`)를
돌립니다. 검사에 하나라도 걸리면 "사람이 따로 챙겨야 하는 상태"가 생겼다는 뜻입니다.

검사는 **숙소 3곳 전부**를, 각 숙소가 선언한 속성을 따라 돕니다 — 바베큐·스파를 이름으로 찾지 않으므로
새 속성이 사전에 추가돼도 같은 검사가 그대로 걸립니다.

검사 항목 14가지: 사전에 없는 속성 · 전체값 누락 · 선택지 밖의 값 · 숫자 범위 밖 · **기준 인원 > 최대 인원** ·
쓰는 값에 요금 없음 · "따로 정함" 표시 = 전체값과 다름 · 자동 계산 필드 = 재계산 결과 · 가려내기 규칙이 가리키는 속성 ·
쓰는 중인데 객실 0 · 사용 여부 필드 = 시설 상태 · 문구 조각 편집 왕복 무손실 · 자동 FAQ 값 확보 ·
**손으로 쓴 답변이 인용한 인원이 이 숙소의 인원인지** · 객실명/코드 중복.

시뮬레이션한 시나리오(전부 통과, error 0건):

| 시나리오 | 확인된 연쇄 |
|---|---|
| 객실 추가 (4층 · 공용BBQ) | 22 → 23객실, 블록 문구·채널 상품 자동 생성, 요금은 옵션에서 상속 |
| 객실 추가 (3층 · 개별BBQ) | 개별BBQ 6 → 7객실, `3층 객실 · 7객실` 자동 재생성 |
| 객실 삭제 2실 | `4~7층 객실 · 20객실`, 선택 상태 초기화 |
| 3층 6실 전체 삭제 | 개별BBQ 블록 `사용중 → 보유·사용안함` 자동 강하, 관련 FAQ 자동 비활성 |
| 시설 이용요금 수정 (스파 무료 → 10,000원) | 28객실 바베큐 요금 **오염 없음** |
| 시설 이용시간 수정 (공용BBQ 17~21 → 16~22) | FAQ Q-0015 답변 동시 갱신, 개별BBQ 영향 없음 |
| 시설 이용시간 수정 (스파 입실~23시 → 22시) | 시작 기준 "입실"과 괄호 조건 유지 |
| 객실 바베큐 옵션 변경 (B401 가스 → 숯불) | 요금이 옵션에서 따라옴, 블록 `바베큐 형태`가 `가스그릴, 숯불`로 재계산 |
| 옵션 요금 수정 (20,000 → 25,000) | 그 옵션 쓰는 6객실 + 블록 요금 문구 동시 갱신 |
| 되돌리기 | 객실·옵션 요금·블록·FAQ 함께 원복 |
| 시설 껐다 켜기 | 상태·객실 값·사용 여부 필드가 함께 이동 |
| 규칙 조각 수정·추가 | 문장 재생성, 기존 규칙 밀리지 않음 |
| **인원을 숫자로 입력** (최대 6 → 9명) | 목록에 없던 값 그대로 저장, 판매 사이트 기준값 `9명 / 6명 / 4명`으로 재계산 |
| **한꺼번에 인원 바꾸기** (7층 4실 기준 4 → 5명) | 4실 동시 반영, 검사 통과 |
| **전체값과 같은 값으로 되돌림** | "따로 정함" 표시 자동 해제 — 표시와 실제가 어긋나지 않음 |
| **가평(개별풀·애견동반)에서 같은 조작** | 바베큐가 없는 숙소에서도 같은 경로로 값·요금·문구가 따라옴 |
| **홍천(캠핑장)에서 캠핑 자리 변경** | 요금표·시설 문구가 `camp_site` 속성에서 자동 계산 |
| **초과입실 문항 + 인원 변경** | 인원을 바꾸면 3문항 답변·시설 값·판매 사이트가 한 번에 이동 |
| **전 객실 인원이 같아짐** | 층 표기(`7층 6명 / 3~6층 4명`)가 사라지고 `6명` 한 값으로 |
| **손으로 쓴 답변이 낡은 인원 인용** | 검사기가 "이 숙소의 인원에 없습니다"로 알림 |
| **숙소 전체값 바꾸기** | 따로 정하지 않은 24실만 따라오고, 따로 정한 4실은 그대로 |
| **전체값을 어떤 객실이 쓰던 값으로** | 그 객실의 "따로 정함"이 자동 해제 |
| **객실 정보 고치기 (층 4 → 8)** | 시설 "이용 객실" 문구가 다시 계산 |
| **객실 복제** | 값·구조·침구까지 그대로, 이름만 다름 |
| **시설 항목 넣고 빼기** | 형식에 맞는 편집기가 붙음 · 자동 계산 항목은 못 뺌 |
| **쓰는 객실 고치기** | 체크 해제한 객실만 값이 비고, 나머지는 종류 유지 |
| **전사 목록에서 시설 가져오기** | 속성·가려내기 규칙·자동 계산이 함께 붙어 바로 계산됨 |
| 연속 10단계 | 마지막까지 어긋난 값 0건 (숙소 3곳 전부 검사) |

### 시뮬레이션이 찾아낸 결함 (수정 완료)

1. **시드 데이터가 이미 낡아 있었습니다.** 저장된 블록 문구(`2인-4인 30,000원…`, `3층 객실 · 6객실`, 스파 `수용인원`)가
   규칙으로 재계산한 값과 달랐습니다. 화면은 계산값을 보여주니 눈에 안 띄지만 저장된 값은 어긋난 상태였습니다.
   → `initialState()`에서 블록을 파생 상태로 저장. 스파 `수용인원`은 층 표기(`7층 4인용 / 3~6층 2인용`)로 재계산하도록 개선.
2. **시설을 다시 켜면 `사용 여부` 필드가 "사용안함"으로 남았습니다.** 카드 배지는 `사용중`, 필드는 `사용안함` —
   정확히 이 콘솔이 없애려는 종류의 모순. → 상태 전환 시 필드가 함께 이동하고, 미리보기에 잠금 행으로 표시됩니다.
3. **초과입실 FAQ 3문항이 인원을 따라오지 않았습니다.** `Q-0011~0013`의 답변은 손으로 쓴 문장이었습니다.
   지금 시드는 숫자를 인용하지 않아 우연히 안 어긋났지만, **초과입실을 허용하는 숙소**(1000곳 중 일부)의
   답변은 "최대 4명까지 가능합니다"처럼 숫자를 인용합니다. 인원을 6명으로 올리면 판매 사이트 값은 계산돼
   따라가는데 답변만 4명에 남아, **한 숙소가 두 가지 말을 하게 됩니다.** 검사기도 자유 입력 답변은 안 봤습니다.
   → `추가 인원` 시설에 `기준 인원`·`최대 인원` 자동 계산 필드를 만들고, 3문항을 그 값에서 생성하도록 바꿨습니다.
   답변을 입력할 곳이 사라졌으므로 어긋날 수 없습니다. 나머지 손으로 쓴 답변을 위해 검사 항목도 하나 늘렸습니다.
4. **시드가 전체값과 같은 값을 객실에 따로 적어 두고 있었습니다.** 속초 12객실의 `객실 비품`이 전체값(`기본 + 테라스`)과
   같은데도 "따로 정함"으로 저장돼 있었습니다. 화면상 차이는 없지만, 나중에 전체값을 바꾸면 **이 12객실만 안 따라옵니다.**
   새 검사 항목("전체값과 같은데 따로 정한 값으로 저장돼 있습니다")이 잡아냈습니다. → 시드에서 다른 값만 적도록 수정.

### 아직 못 하는 것 (설계 판단이 필요합니다)

1. **이용 시간은 시설 단위입니다.** "3층 15시 / 4층 17시"처럼 층·객실별로 다르게 두려면 이용 시간이
   바베큐처럼 **객실 값**으로 내려가야 합니다. 지금 구조에서는 공용BBQ 시설 하나가 4~7층 전체를 덮습니다.
   속성 사전에 `bbq_hours` 같은 항목을 넣는 것으로 풀 수 있지만, 시설 카드와 객실 표 중 어디를 정본으로 볼지
   판단이 필요합니다.
2. **FAQ 답변을 입력할 곳이 없습니다.** 시설 값을 인용하는 10건은 자동 생성이라 문제없지만, 나머지 문항의 답변은
   읽기 전용입니다. 시설을 새로 켜면 검사기가 `답변이 비어 채널 전송에서 빠지는 문항 N건`으로 알려주지만,
   그 자리에서 채울 UI는 없습니다.
3. **한 시설에 옵션이 섞이면 요금 문구가 나열됩니다.** 객실마다 다른 바베큐 옵션을 쓰면 블록 `이용 요금`이
   `2~4인 30,000원… / 1세트 38,000원…`처럼 이어붙습니다. 채널에 그대로 보낼 문장인지 판단이 필요합니다.

## 쉬운 말·쉬운 절차 (초등학생 기준)

"초등학생도 어렵지 않게 고칠 수 있어야 한다"는 기준으로 다시 손봤습니다.

**절차** — `행 체크 → 일괄 편집 → 필드 → 값 → 미리보기 → 적용`(클릭 6번, 창 2개)이
`칸 누르기 → 값 고르기`(클릭 2번, 창 0개)가 됐습니다.

- 객실 표의 값 칸을 누르면 그 자리에서 창이 뜹니다(`CellPicker`). 브라우저 popover라 top layer에
  그려져 표 스크롤에 잘리지 않습니다. **값의 종류가 무엇을 띄울지 정합니다** —
  선택지면 목록(각 항목에 요금이 함께 보임), 인원·요금이면 −/+ 버튼이 달린 숫자 입력칸(Enter로 저장)
- **값 변경은 확인 창 없이 바로 반영**되고, 화면 아래에 문장으로 알립니다:
  "객실 1개의 바베큐 유형을 이용 불가로 바꿨어요. 시설 안내문 2곳도 같이 바뀌었어요." + 되돌리기
- **확인 창은 되돌리기로 못 되살리는 것에만** 뜹니다 — 객실 만들기·지우기, 시설 켜고 끄기,
  그리고 여러 객실의 *따로 정해둔 값*을 덮어쓸 때(`domain/summary.ts`의 `needsConfirm`)
- 확인 창도 표가 아니라 **문장이 먼저**입니다. 고를 것이 없으면 목록은 접혀 있습니다
- 조사(을/를 · 으로/로)를 받침에 맞춰 붙입니다 — "바베큐 유형를"처럼 어긋나면 읽는 사람이 먼저 걸립니다

**말** — 만든 사람의 말을 쓰는 사람의 말로 바꿨습니다.

| 전 | 후 |
|---|---|
| 오버라이드 | 이 객실만 따로 정함 |
| 숙소 기본값 상속 | 전체와 같음 |
| 연쇄 갱신 미리보기 | 이렇게 바뀝니다 |
| 자동 산출 | 자동 계산 |
| 시설·블록정보 / 옵션·요금 / 채널 매핑 | 시설 정보 / 요금표 / 판매 사이트 |
| 변경 이력 / FAQ | 바꾼 기록 / 자주 묻는 질문 |
| 적용하고 저장 / 취소 | 네, 바꿀게요 / 그만두기 |
| 마스터값으로 교정 | 기준값으로 맞추기 |
| 사용중 / 보유·사용안함 / 미보유 | 쓰는 중 / 있지만 안 씀 / 없음 |

옵션 코드(`shared_gas`)는 요금표 탭에만 남기고, 객실코드는 흐리게 내렸습니다.

**아직 남은 복잡함** — 화면 구조(3단 · 탭 6개 · 10열)는 이번 범위에서 건드리지 않았습니다.
좌측 목록과 우측 설명창이 1440 중 614px(43%)를 차지하는 것, 탭 6개의 성격이 제각각인 것은
그대로입니다. 다음에 손댈 후보입니다.

## 1000개를 담는 구조 — 무엇을 바꿨나

기존 구조는 **속초 한 곳의 모양에 박혀 있었습니다.** `Room` 타입에 `bbq`·`spa`가 열로 있었고, 인원은
`['4','6','8']` 고정 목록이었고, 시설 계산은 `shared_bbq` 키를 코드에서 직접 찾았습니다 —
하드코딩된 참조가 12개 파일에 78곳이었습니다.

| | 전 | 후 |
|---|---|---|
| 객실이 가지는 값 | `Room` 타입에 열로 박힘 | 전사 속성 사전 + 숙소별 `attrs` 부분집합 |
| 객실 저장 방식 | 모든 필드 + `~Ov` 불리언 | `values`에 **다른 것만** (있으면 곧 "따로 정함") |
| 인원 | 3개짜리 고정 목록 | 숫자 입력 (min/max는 사전이 정의) |
| 요금 | `optFees[code]` (바베큐 전용) | `fees['속성:선택지']` — 요금이 붙는 속성 전부 |
| 시설↔객실 연결 | 코드에서 `shared_bbq` 문자열 검색 | 시설이 `memberOf: { attr, codes }`를 선언 |
| 자동 계산 | 블록 키별 분기 | 시설이 `computed: { 필드: 종류 }`를 선언 |
| 판매 사이트 변환 | 별도 매핑 테이블 | 선택지마다 붙은 사전(`ch`)에서 계산 |
| 검사 | 바베큐 하드코딩 10항목 | 속성을 따라 도는 13항목 × 숙소 전부 |

새 숙소 유형을 붙이는 데 드는 일: `attrs.ts`에 속성 한 줄 + 시드에 숙소 하나. 화면 코드는 건드리지 않습니다.

## 객실 탭 · 시설 정보 탭에서 할 수 있는 일

두 탭에서 막혀 있던 9가지를 채웠습니다.

**객실 탭**

| | 어떻게 |
|---|---|
| 값 고치기 | 칸을 눌러 그 자리에서 (선택지는 목록, 인원·요금은 숫자) |
| **여러 객실 한꺼번에** | 한 창에서 왼쪽 층 트리로 고르고 → 오른쪽에 쌓이고 → 아래에서 값. 일괄값을 넣되 **객실마다 다르게** 덮어쓸 수 있습니다 |
| **숙소 전체값 고치기** | 오른쪽 패널의 값을 누름 — 따로 정한 객실은 그대로 두고 나머지만 따라옴 |
| **객실 정보 고치기** | 줄에 마우스를 올리면 나오는 "정보" (객실명·층·면적·구조·침구·특징) |
| **객실 복제** | 줄의 "복제" — 값·구조·침구까지 그대로 가져옴 |
| 객실 만들기 | 이 숙소에서 가장 흔한 모양이 미리 채워짐 |
| 객실 지우기 | 체크 → 지우기 |
| **정렬·필터** | 층순 / 이름순 / 따로 정함 먼저 · "따로 정한 객실만 보기" |
| **층으로 고르기** | `3층 6` 칩 한 번 = 체크박스 6번. 다시 누르면 해제 |
| **같은 값 쓰는 객실 모두 고르기** | 값 칸을 눌렀을 때 나오는 지름길 — 공용BBQ 22실이 두 번 클릭 |
| **객실명 고정** | 가로로 스크롤해도 어느 객실인지 사라지지 않음 |

**시설 정보 탭**

| | 어떻게 |
|---|---|
| 항목 값 고치기 | 형식에 맞는 편집기 (시각·시간대·금액·구간·정수·선택지) |
| **항목 넣기·빼기** | 전사 항목 목록에서 고름 · 자동 계산 항목은 뺄 수 없음 |
| 안내 문구 넣기·빼기·조각 고치기 | 전사 문구 목록에서 고르고 숫자만 채움 |
| **쓰는 객실 고치기** | 체크한 객실에만 붙음 · 체크를 풀면 그 객실에서 빠짐 |
| 시설 켜기·끄기·없애기 | 상태·객실 값·사용 여부 필드가 함께 이동 |
| **시설 찾기** | 이름·항목·문구까지 훑어서 19개 중 좁힘 (검색칸은 숙소 검색과 따로) |
| **전사 목록에서 가져오기** | 속성·가려내기 규칙·자동 계산이 함께 붙어 **바로 계산됨** |

전체값 편집이 없던 것이 가장 컸습니다 — 상속 구조의 절반(전체값 → 객실)이 읽기 전용이라
"전체값을 바꾸면 나머지가 따라온다"를 화면에서 할 수 없었습니다.

## 남은 작업

- 속성 사전 확장 후보: 침대 타입, 객실 형태, 수영장 온도, 객실내 시설 19항목. 지금은 `attrs.ts`에 항목만 추가하면 됩니다.
- **속성 사전을 화면에서 관리하기.** 지금은 코드 상수입니다. 1000개를 운영팀이 직접 다루려면
  "이 숙소에 캠핑 자리 추가" 같은 조작이 화면에 있어야 합니다.
- 일관성 검사(`audit.ts`)를 화면에도 띄우기 — 지금은 시뮬레이션에서만 돌립니다. 헤더에 "검사 N건" 배지를 붙이면
  판매 사이트 전송 전에 걸러집니다.
- 서버 연동 시: `domain/`은 그대로 두고 `state/store.tsx`의 commit 경로만 API 호출로 바꾸면 됩니다.
- `npm run bundle`은 CSP가 엄격한 호스트(외부 CSS·JS·폰트 요청 차단)에 얹을 단일 HTML을 만듭니다. 폰트는
  `scripts/fonts-inline.css`에 data URI로 들어 있고(Barlow, OFL), 스크립트가 토큰 블록 존재와 잔여 `@import`를
  검사해 스타일이 조용히 깨진 채 나가는 것을 막습니다.
